arreglar make re

```bash
# cat | cat | echo hola

# export TEST="echo hola"  ---> Idea: usar el modo no interactivo para correr $TEST
# $TEST --> hola

# yes | head -n 20

# cat << EOF << OEF << FEO > test.txt # solo se guarda el ultimo
# exit
# exit | exit
# exit 4 | exit 3

# (echo hola && echo adios) || echo uuh
# (ecro hola && echy adios) || echo uuh
# (echo hola && echy adios) || echo uuh

```

